import React from 'react'
import {StyledNav} from '../styles/Nav.style'

const Nav = () => {
  return (
    <StyledNav>
        <nav>
            <div>
                <a href="#">Home</a>
                <a href="#">Partner</a>
                <a href="#">About</a>
                <a href="#">Help</a>
                <a href="#">FAQs</a>
            </div>
        </nav>
    </StyledNav>
  )
}

export default Nav